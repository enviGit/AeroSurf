<Window x:Class="AeroSurf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        Title="{Binding Title}" Height="720" Width="1080"
        WindowStyle="None" ResizeMode="CanResize" WindowState="Maximized"
        Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1"
        Icon="/icon.ico">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" ResizeBorderThickness="5" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="ModernBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="34"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E3E"/>
                </Trigger>

                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#007ACC"/>
                </Trigger>

                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#444444"/>

                    <Setter Property="Background" Value="Transparent"/>

                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WindowControlBtn" TargetType="Button" BasedOn="{StaticResource ModernBtn}">
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
        </Style>
        <Style x:Key="CloseBtn" TargetType="Button" BasedOn="{StaticResource WindowControlBtn}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#252526" MouseLeftButtonDown="TitleBar_MouseDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="AeroSurf" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="15,0,0,0" FontSize="12" FontWeight="SemiBold"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Style="{StaticResource WindowControlBtn}" Click="Minimize_Click" Content="_"/>
                <Button Style="{StaticResource WindowControlBtn}" Click="Maximize_Click" Content="☐"/>
                <Button Style="{StaticResource CloseBtn}" Click="Close_Click" Content="✕"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="#2D2D30" BorderBrush="#3E3E42" BorderThickness="0,0,0,1">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Command="{Binding GoBackCommand}" CommandParameter="{Binding ElementName=Browser}" Style="{StaticResource ModernBtn}" Focusable="False" ToolTip="Wstecz">
                    <Path Data="M20,11V13H8L13.5,18.5L12.08,19.92L4.16,12L12.08,4.08L13.5,5.5L8,11H20Z" 
                          Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" 
                          Stretch="Uniform" Height="16" Width="16"/>
                </Button>

                <Button Grid.Column="1" Command="{Binding GoForwardCommand}" CommandParameter="{Binding ElementName=Browser}" Style="{StaticResource ModernBtn}" Focusable="False" ToolTip="Dalej">
                    <Path Data="M4,11V13H16L10.5,18.5L11.92,19.92L19.84,12L11.92,4.08L10.5,5.5L16,11H4Z" 
                          Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" 
                          Stretch="Uniform" Height="16" Width="16"/>
                </Button>

                <Button Grid.Column="2" Command="{Binding ReloadCommand}" CommandParameter="{Binding ElementName=Browser}" Style="{StaticResource ModernBtn}" Focusable="False" ToolTip="Odśwież">
                    <Path Data="M17.65 6.35C16.2 4.9 14.21 4 12 4c-4.42 0-7.99 3.58-7.99 8s3.57 8 7.99 8c3.73 0 6.84-2.55 7.73-6h-2.08c-.82 2.33-3.04 4-5.65 4-3.31 0-6-2.69-6-6s2.69-6 6-6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z" 
                          Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" 
                          Stretch="Uniform" Height="16" Width="16"/>
                </Button>

                <Border Grid.Column="3" Background="#1E1E1E" CornerRadius="15" Margin="10,2" BorderBrush="#3E3E3E" BorderThickness="1">
                    <TextBox x:Name="UrlTextBox"
                             Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" 
                             Background="Transparent" Foreground="White" BorderThickness="0"
                             VerticalContentAlignment="Center" Padding="10,0" FontSize="14" CaretBrush="White">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchCommand}" CommandParameter="{Binding ElementName=Browser}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                </Border>

                <Button Grid.Column="4" Content="GO" 
                        Command="{Binding SearchCommand}" 
                        CommandParameter="{Binding ElementName=Browser}" 
                        Style="{StaticResource ModernBtn}" 
                        Width="50" Margin="5,0,0,0" FontWeight="Bold"/>
            </Grid>
        </Border>

        <cef:ChromiumWebBrowser x:Name="Browser" 
                                Grid.Row="2"
                                Title="{Binding Title, Mode=OneWayToSource}">
        </cef:ChromiumWebBrowser>
    </Grid>
</Window>